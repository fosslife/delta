 - auto deleting files? based on fixed time maybe
 - Live reload config

add readme `up` method of usage
    edit hardoded apikey in up.sh
make asciinema for latest demo
give more options for databases (nedb? sqlite)
check duplicate custom urls
logging not working probably?
cache earlier X redirects?